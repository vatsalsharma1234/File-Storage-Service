# File-Storage-Service
A lightweight backend service that allows users to **upload, download, list, and delete files** while supporting **file deduplication using SHA-256 hashing**.  This project demonstrates core backend concepts such as **file storage systems, hashing, REST APIs, and metadata management**.
